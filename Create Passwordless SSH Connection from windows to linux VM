# Windows → Ubuntu SSH Key Setup (Short Steps)

Step 1: 

Create SSH Key on Windows**
Open PowerShell:

```powershell
ssh-keygen -t ed25519 -C "sultantalib@windows"
ls $env:USERPROFILE\.ssh

Step 2:

type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh sultantalib@192.168.0.31 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
** chnage the IP ADDRESS OF THE MACHINE 

